<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
  <title><%= blog.title %></title>
  <link rel="stylesheet" type="text/css" href="/css/blog-ui.css"></head>
<body>
  <div class="container">
  <% if(!user) { %>
  <header class="header">
		<h3 class="text-muted"><%= blog.title %></h3>
    <p><%= blog.description %></p>
	</header>
  <% }else{ %>
  <header class="header row">
      <div class="col-lg-2"> <a href="/posts?author=<%= user._id %>" class="avatar" data-toggle="tooltip" data-placement="bottom" title="<%= user.name %> | <%= ({m: '男', f: '女', x: '保密'})[user.gender] %>"><img src="/img/<%= user.avatar %>"></a>
      </div>
      <div class="col-lg-10">
        <h3 class="text-muted"><%= user.name %></h3>
        <p><%= user.bio %></p>
      </div>
  </header>
    <% } %>
    <%- include('components/nav') %>
    <%- include('components/notification') %>
  